import Navbar from "./components/Navbar";

function App() {
    return (
        <div className="min-h-screen bg-white">
            <Navbar />
            {/* বাকি সেকশনগুলো (Hero, Cards, Footer) পরের ধাপে এখানে যোগ হবে */}
        </div>
    );
}

export default App;